- name: loops
  hosts: localhost
  become: yes
  tasks:
  - name: print persons
    ansible.builtin.package:
      name: "{{item}}""
      state: present
     # msg: "{{item}}" # item is default value for loops
    loop:
    - git
    - nginx
    